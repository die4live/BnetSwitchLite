# 更新记录

本文件记录 fork（`die4live/BnetSwitchLite`）之后相对于上游 `kabxx/BnetSwitchLite` 的全部改动，按版本号倒序排列。

上游基线：**v1.0.0**（导入点 commit `c35f4ac`，此后上游未再合并）。

版本号同时出现在三处：窗口标题栏、GitHub Release 页、`package.json` 与 `src-tauri/tauri.conf.json`。

---

## v1.0.3 — 2026-09-13

### 窗口与布局

- **窗口最小宽度 240 → 180 px**。账号行在窄窗口下的物理下限约 178 px（外层内边距 16 + 卡片边框 2 + 行内边距 24 + 头像 36 + 间距 8 + 切换按钮 92），180 px 时中间空隙归零。
- **修复「缩到约 360 px 后整页空白」**：`account-row.tsx` 中 `tipOpen` 状态漏了声明，进入 ≤360 px 分支时抛出 `ReferenceError`，React 卸载整棵组件树；表现为整页空白且所有元素都查不到（并非裁切或 CSS 问题）。
- **修复「切换按钮 / 工具栏图标跑到窗外」**：`body` 写死的 `min-width: 320px` 顶穿了已降到 240 px 的窗口宽度，右侧内容被整块裁掉。改为 160 px。

### 通知条（toast）

- 高度锁定为 **40 px**，与底部工具栏等高，贴底时整条覆盖「启动战网 / 刷新账号」那一行。
- 移除关闭按钮：窄窗口下叉号会溢出框体。
- 修正 padding 一直压不掉的问题——规则原写在 Tailwind 的 `@layer components` 内，打不过第三方运行时注入的未分层样式，特异性再高也无效。

### 确认弹窗

- **由系统原生对话框改为应用内弹窗**。原生框固定居中于屏幕且不跟随窗口，窄窗口下会跑到窗口外；应用内弹窗天然居中于应用窗口。
- 危险操作（移除账号）显示红色确认按钮，且初始焦点落在「取消」以防误触。
- 交互语义：Esc 等同取消；点击弹窗外不关闭（确认框必须选一个）。

### 窄窗口提示

- 账号信息在 ≤360 px 被隐藏时，头像支持**悬停或点击**弹出提示，内容为账号名 / 服务器 / 更新时间；悬停 60 ms 弹出、移开立即消失。

### 窗口行为

- **最小化即隐藏到系统托盘**：托盘图标左键恢复窗口，右键菜单提供「显示窗口 / 退出」。
- **记住窗口位置与大小**：拖动 / 缩放停止约 1.2 秒后防抖保存，关闭时同步落盘，下次启动显示前恢复。过滤 Windows 最小化产生的 `-32000` 坐标；窗口完全落在屏幕外时只恢复尺寸并回到居中。

### 开发与工程

- 新增**浏览器预览模式**：非 Tauri 环境（`npm run dev`）用假数据渲染界面，便于在浏览器 devtools 中检查布局；Tauri 运行时不受影响。
- **修正 `npm run typecheck`**：原命令 `tsc --noEmit` 因根 tsconfig 是 `files: []` + references 形态而**不检查任何文件、恒定通过**，CI 的同名门禁形同虚设。现改为显式指定两个子配置进行真实检查。
- Rust 侧启用 tauri 的 `tray-icon` feature；`Cargo.lock` 无需变更（lockfile 本就记录全部可选依赖闭包，`--locked` 不受影响）。

---

## v1.0.2 — 2026-09-13

- 窄窗口进一步精简：**≤420 px** 底部工具栏按钮只保留图标（隐藏文字），**≤360 px** 隐藏账号名与移除按钮、操作区收为单列。
- 窗口最小宽度 **320 → 240 px**。
- 切换确认弹窗带上账号所在**服务器**，避免切换不同区域的同名账号时选错。
- 通知条由「停在工具栏上方」改为**贴底覆盖**底部工具栏。

---

## v1.0.1 — 2026-09-13

- 标题栏显示版本号：**战网切号器 vX.Y.Z**。版本号取自 `tauri.conf.json`，后续 bump 无需改动代码。
- 修复 `clippy::unnecessary_sort_by`：快照历史排序改为 `sort_by_key(Reverse(..))`。
- 新增 CI 质量门禁并与构建同 job：typecheck / eslint / prettier / `cargo fmt --check` / `clippy -D warnings` / `cargo test`。
- 前端代码全量 prettier 格式化（启用格式门禁的前置修复）。

---

## v1.0.0-fork.2 — 2026-09-13

- 空状态加入三步使用指引：选择战网客户端 → 启动战网登录 → 刷新账号。
- 窄窗口自适应（420 / 360 px 断点），窗口最小宽度 **480 → 320 px**。
- 通知条改为贴底显示。
- 当前已登录的账号在列表中置灰为「已登录」，不再作为可切换目标。

---

## v1.0.0-fork.1 — 2026-09-13

- 首次形成可发布形态：CI 在 push tag 时构建 **Windows 免安装单文件 exe** 并自动创建 Release（附 SHA256 校验值）。
- 新增提交前凭据拦截 hook（`.githooks/pre-commit`，需先执行 `git config core.hooksPath .githooks`），防止快照 / 令牌类文件误入版本库。
- 接入 Dependabot（cargo / npm / actions）与 CodeQL（TypeScript + Rust）。
- 新增 `README-FORK.md`，说明 fork 用途、构建方式与数据目录位置。
- 版本号 1.0.0 → 1.0.0-fork.1。

---

## 关于版本号

- 版本号只改 `package.json` 与 `src-tauri/tauri.conf.json` 两处。
- `src-tauri/Cargo.toml` 的 `version` 保持不变：改动它会连带改写 `Cargo.lock`，而 CI 使用 `--locked`，不同步会直接失败。因此代码中取版本一律读 `app.config().version`。
- 打包脚本的 `ValidatePattern` 不支持 `+`，版本分隔符只能用 `-` 或 `.`。
